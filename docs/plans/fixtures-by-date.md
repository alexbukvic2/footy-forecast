# Plan: Fixtures by Date

## Goal

Extend the existing `GET /leagues/{leagueId}/predictions` endpoint with an
optional `dates[]` query parameter so callers can filter fixtures to one or more
calendar dates, rather than always receiving the full list of locked fixtures.

---

## Out of Scope

- Adding a brand-new endpoint (we extend the existing one).
- Cross-league or global (non-league) fixture-by-date queries.
- Pagination or cursor-based results.
- Returning the caller's own upcoming (pre-lock) prediction alongside locked
  member predictions (may be addressed in a follow-up).

---

## Data Model Changes

No schema migrations required. The existing `fixtures`, `teams`,
`league_members`, `users`, and `score_predictions` tables already contain all
needed data.

The existing `ListLockedFixturesByLeague` query in
`internal/repository/queries/fixtures.sql` is extended (or a companion query
`ListLockedFixturesByLeagueAndDates` is added) to accept an optional
`dates::date[]` array:

- When `dates` is non-empty: append `AND kickoff_at::date = ANY(@dates::date[])`.
- When `dates` is empty/nil: behave exactly as today (no extra filter).

The repository method signature changes to accept `[]time.Time` (dates); the
service passes nil when no dates are provided.

---

## API Contract

### Modified route

**`GET /leagues/{leagueId}/predictions`**

| | |
|---|---|
| Auth | `bearerAuth` (protected) — unchanged |
| Path param | `leagueId` — UUID of the league — unchanged |
| Query param (new) | `dates[]` — zero or more dates in `YYYY-MM-DD` format (repeatable: `?dates[]=2026-06-14&dates[]=2026-06-15`). Omitting `dates[]` returns all locked fixtures, exactly as before. |

**Response `200 OK`** — array of `LeagueFixtureViewResponse` — **unchanged schema**.

The existing `LeagueFixtureViewResponse` shape already matches the requested
fields (`home_team_name`, `away_team_name`, `kickoff_at`, `goals_home`,
`goals_away`, `status`) and `predictions` array items
(`LeagueMemberScorePrediction`) already carry `display_name`, `goals_home`,
`goals_away`, `points`. No schema additions needed.

**New error responses** (all `$ref: '#/components/schemas/ErrorResponse'`):

| Condition | Status | Body |
|-----------|--------|------|
| Non-`YYYY-MM-DD` value in `dates[]` | 400 | `{"error": "invalid date format, expected YYYY-MM-DD"}` |
| More than 7 dates | 400 | `{"error": "too many dates, maximum is 7"}` |

Existing error codes (`401`, `403`, `500`) are unchanged.

**Notes:**
- Omitting `dates[]` entirely preserves existing behaviour (all locked fixtures
  returned) — this is a **backward-compatible** change.
- The 7-date cap is enforced at the handler layer before hitting the service.
- Dates are interpreted in UTC (`kickoff_at::date` in Postgres default timezone).

> **Implementer:** add the `dates[]` query parameter and the two new 400 cases
> to `docs/openapi.yaml` under `/leagues/{leagueId}/predictions`, then run
> `make generate` before touching any handler code.

---

## Touched Files

| File | Change |
|---|---|
| `docs/openapi.yaml` | Add `dates[]` query param + two new 400 error descriptions to `GET /leagues/{leagueId}/predictions` |
| `docs/openapi.json` | Regenerated by `make generate` |
| `internal/server/oapi/models.gen.go` | Regenerated by `make generate` |
| `internal/repository/queries/fixtures.sql` | Extend `ListLockedFixturesByLeague` (or add `ListLockedFixturesByLeagueAndDates`) to accept an optional dates array |
| `internal/repository/dbgen/` | Regenerated by `make generate` (sqlc) |
| `internal/repository/fixture.go` | Update `ListLockedByLeague` signature to accept `[]time.Time` dates (nil = no filter); update interface |
| `internal/service/score_prediction.go` | Pass dates through to repository; update `FixtureForUserLister` interface |
| `internal/server/handler/score_prediction.go` | Parse `dates[]` query params, validate, pass to service |
| `postman/collections/footy-forecast/leagues/` | Update `ListLeagueScorePredictions.request.yaml` to show example with `dates[]` |

---

## Edge Cases

1. **`dates[]` omitted** — nil slice passed to repo; query runs without date
   filter, returning all locked fixtures. Existing behaviour preserved.
2. **Empty string date** (`dates[]=`) — caught by `time.Parse`, return `400`.
3. **Malformed date** (e.g. `2026-6-1`, `yesterday`) — `time.Parse("2006-01-02", …)`
   fails, return `400`.
4. **More than 7 dates** — return `400` before touching the DB.
5. **Valid dates but no fixtures on those dates** — return `200 []`.
6. **League not found / caller not a member** — existing `403`/`404` behaviour
   unchanged; date parsing happens before any DB call.
7. **All specified dates are in the future** — fixtures are `upcoming`; since the
   existing endpoint only returns *locked* fixtures, result is `200 []`. The date
   filter does not change the locked-only constraint.

---

## Test Plan

### Repository (`internal/repository/`)

- `TestFixtureRepository_ListLockedByLeague_NoDatesFilter` — existing happy-path
  test continues to pass (nil dates = no filter).
- `TestFixtureRepository_ListLockedByLeague_WithDates` — seed two locked fixtures
  on different dates, request one date, assert only that fixture is returned.
- `TestFixtureRepository_ListLockedByLeague_MultipleDates` — request both dates,
  assert both fixtures returned.
- `TestFixtureRepository_ListLockedByLeague_NoMatch` — date with no locked
  fixtures returns empty slice, no error.

### Service (`internal/service/`)

- Existing service tests continue to pass with `nil` dates.
- `TestFixtureService_ListForLeague_WithDates_HappyPath` — fake repo receives
  expected dates; returns correct views.

### Handler (`internal/server/handler/`)

- Existing handler tests continue to pass (no `dates[]` in request).
- `TestFixtureHandler_ListByDates_ValidDates` — `?dates[]=2026-06-14`, 200 + correct JSON.
- `TestFixtureHandler_ListByDates_InvalidDate` — non-date string, 400.
- `TestFixtureHandler_ListByDates_TooManyDates` — 8 dates, 400.

---

## Open Questions

1. **Locked-only constraint with dates filter** — Today the endpoint only returns
   `in_progress`/`finished` fixtures. Should date-filtered requests also include
   `upcoming` fixtures on those dates? The current plan keeps the locked-only
   constraint (safest, no behaviour change). If upcoming fixtures should be
   visible when a date is provided, the SQL filter and the endpoint description
   both need updating — please confirm.

2. **Date timezone** — Dates are matched as `kickoff_at::date` in UTC (Postgres
   default). If users in non-UTC timezones need "their" local date, a timezone
   offset parameter may be needed. Recommend deferring until requirements are
   clearer.
