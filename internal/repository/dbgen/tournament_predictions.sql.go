// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: tournament_predictions.sql

package dbgen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const countPlayoffWildcards = `-- name: CountPlayoffWildcards :one
SELECT COUNT(*)::int FROM team_predictions
WHERE tournament_id = $1
  AND user_id       = $2
  AND category      = 'playoff'
  AND slot_index    = 2
`

type CountPlayoffWildcardsParams struct {
	TournamentID uuid.UUID
	UserID       uuid.UUID
}

func (q *Queries) CountPlayoffWildcards(ctx context.Context, arg CountPlayoffWildcardsParams) (int32, error) {
	row := q.db.QueryRow(ctx, countPlayoffWildcards, arg.TournamentID, arg.UserID)
	var column_1 int32
	err := row.Scan(&column_1)
	return column_1, err
}

const listLeagueMembersForPredictions = `-- name: ListLeagueMembersForPredictions :many
SELECT lm.user_id, u.display_name
FROM league_members lm
JOIN users u ON u.id = lm.user_id
WHERE lm.league_id = $1
ORDER BY u.display_name ASC
`

type ListLeagueMembersForPredictionsRow struct {
	UserID      uuid.UUID
	DisplayName string
}

func (q *Queries) ListLeagueMembersForPredictions(ctx context.Context, leagueID uuid.UUID) ([]ListLeagueMembersForPredictionsRow, error) {
	rows, err := q.db.Query(ctx, listLeagueMembersForPredictions, leagueID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListLeagueMembersForPredictionsRow{}
	for rows.Next() {
		var i ListLeagueMembersForPredictionsRow
		if err := rows.Scan(&i.UserID, &i.DisplayName); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPlayerPredictionsByLeague = `-- name: ListPlayerPredictionsByLeague :many
SELECT lm.user_id, pp.category, pp.group_letter, pp.pick AS player_id,
       p.name AS player_name, pp.points
FROM league_members lm
JOIN leagues l ON l.id = lm.league_id
JOIN player_predictions pp
    ON pp.user_id = lm.user_id AND pp.tournament_id = l.tournament_id
JOIN players p ON p.id = pp.pick
WHERE lm.league_id = $1
`

type ListPlayerPredictionsByLeagueRow struct {
	UserID      uuid.UUID
	Category    PlayerHandicapCategory
	GroupLetter *string
	PlayerID    uuid.UUID
	PlayerName  string
	Points      *int32
}

func (q *Queries) ListPlayerPredictionsByLeague(ctx context.Context, leagueID uuid.UUID) ([]ListPlayerPredictionsByLeagueRow, error) {
	rows, err := q.db.Query(ctx, listPlayerPredictionsByLeague, leagueID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListPlayerPredictionsByLeagueRow{}
	for rows.Next() {
		var i ListPlayerPredictionsByLeagueRow
		if err := rows.Scan(
			&i.UserID,
			&i.Category,
			&i.GroupLetter,
			&i.PlayerID,
			&i.PlayerName,
			&i.Points,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPlayerPredictionsByTournamentForUser = `-- name: ListPlayerPredictionsByTournamentForUser :many
SELECT pp.id, pp.user_id, pp.tournament_id, pp.category, pp.pick,
       pp.group_letter, p.name AS pick_name, pp.points, pp.created_at, pp.updated_at
FROM player_predictions pp
JOIN players p ON p.id = pp.pick
WHERE pp.tournament_id = $1 AND pp.user_id = $2
`

type ListPlayerPredictionsByTournamentForUserParams struct {
	TournamentID uuid.UUID
	UserID       uuid.UUID
}

type ListPlayerPredictionsByTournamentForUserRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	TournamentID uuid.UUID
	Category     PlayerHandicapCategory
	Pick         uuid.UUID
	GroupLetter  *string
	PickName     string
	Points       *int32
	CreatedAt    time.Time
	UpdatedAt    time.Time
}

func (q *Queries) ListPlayerPredictionsByTournamentForUser(ctx context.Context, arg ListPlayerPredictionsByTournamentForUserParams) ([]ListPlayerPredictionsByTournamentForUserRow, error) {
	rows, err := q.db.Query(ctx, listPlayerPredictionsByTournamentForUser, arg.TournamentID, arg.UserID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListPlayerPredictionsByTournamentForUserRow{}
	for rows.Next() {
		var i ListPlayerPredictionsByTournamentForUserRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.TournamentID,
			&i.Category,
			&i.Pick,
			&i.GroupLetter,
			&i.PickName,
			&i.Points,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamPredictionsByLeague = `-- name: ListTeamPredictionsByLeague :many
SELECT lm.user_id, tp.category, tp.group_letter, tp.slot_index,
       tp.pick AS team_id, t.name AS team_name, tp.points
FROM league_members lm
JOIN leagues l ON l.id = lm.league_id
JOIN team_predictions tp
    ON tp.user_id = lm.user_id AND tp.tournament_id = l.tournament_id
JOIN teams t ON t.id = tp.pick
WHERE lm.league_id = $1
`

type ListTeamPredictionsByLeagueRow struct {
	UserID      uuid.UUID
	Category    TeamHandicapCategory
	GroupLetter *string
	SlotIndex   int16
	TeamID      uuid.UUID
	TeamName    string
	Points      *int32
}

func (q *Queries) ListTeamPredictionsByLeague(ctx context.Context, leagueID uuid.UUID) ([]ListTeamPredictionsByLeagueRow, error) {
	rows, err := q.db.Query(ctx, listTeamPredictionsByLeague, leagueID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListTeamPredictionsByLeagueRow{}
	for rows.Next() {
		var i ListTeamPredictionsByLeagueRow
		if err := rows.Scan(
			&i.UserID,
			&i.Category,
			&i.GroupLetter,
			&i.SlotIndex,
			&i.TeamID,
			&i.TeamName,
			&i.Points,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamPredictionsByTournamentForUser = `-- name: ListTeamPredictionsByTournamentForUser :many
SELECT tp.id, tp.user_id, tp.tournament_id, tp.category, tp.pick,
       tp.group_letter, tp.slot_index, t.name AS pick_name,
       tp.points, tp.created_at, tp.updated_at
FROM team_predictions tp
JOIN teams t ON t.id = tp.pick
WHERE tp.tournament_id = $1 AND tp.user_id = $2
`

type ListTeamPredictionsByTournamentForUserParams struct {
	TournamentID uuid.UUID
	UserID       uuid.UUID
}

type ListTeamPredictionsByTournamentForUserRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	TournamentID uuid.UUID
	Category     TeamHandicapCategory
	Pick         uuid.UUID
	GroupLetter  *string
	SlotIndex    int16
	PickName     string
	Points       *int32
	CreatedAt    time.Time
	UpdatedAt    time.Time
}

func (q *Queries) ListTeamPredictionsByTournamentForUser(ctx context.Context, arg ListTeamPredictionsByTournamentForUserParams) ([]ListTeamPredictionsByTournamentForUserRow, error) {
	rows, err := q.db.Query(ctx, listTeamPredictionsByTournamentForUser, arg.TournamentID, arg.UserID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListTeamPredictionsByTournamentForUserRow{}
	for rows.Next() {
		var i ListTeamPredictionsByTournamentForUserRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.TournamentID,
			&i.Category,
			&i.Pick,
			&i.GroupLetter,
			&i.SlotIndex,
			&i.PickName,
			&i.Points,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertPlayerPredictionGroup = `-- name: UpsertPlayerPredictionGroup :one
WITH ins AS (
    INSERT INTO player_predictions (user_id, tournament_id, category, pick, group_letter)
    VALUES ($1, $2, $3, $4, $5)
    ON CONFLICT (user_id, tournament_id, category, group_letter)
        WHERE group_letter IS NOT NULL
    DO UPDATE SET pick = EXCLUDED.pick, updated_at = now()
    RETURNING id, user_id, tournament_id, category, pick, group_letter, points, created_at, updated_at
)
SELECT ins.id, ins.user_id, ins.tournament_id, ins.category, ins.pick,
       ins.group_letter, p.name AS pick_name, ins.points, ins.created_at, ins.updated_at
FROM ins JOIN players p ON p.id = ins.pick
`

type UpsertPlayerPredictionGroupParams struct {
	UserID       uuid.UUID
	TournamentID uuid.UUID
	Category     PlayerHandicapCategory
	Pick         uuid.UUID
	GroupLetter  *string
}

type UpsertPlayerPredictionGroupRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	TournamentID uuid.UUID
	Category     PlayerHandicapCategory
	Pick         uuid.UUID
	GroupLetter  *string
	PickName     string
	Points       *int32
	CreatedAt    time.Time
	UpdatedAt    time.Time
}

func (q *Queries) UpsertPlayerPredictionGroup(ctx context.Context, arg UpsertPlayerPredictionGroupParams) (UpsertPlayerPredictionGroupRow, error) {
	row := q.db.QueryRow(ctx, upsertPlayerPredictionGroup,
		arg.UserID,
		arg.TournamentID,
		arg.Category,
		arg.Pick,
		arg.GroupLetter,
	)
	var i UpsertPlayerPredictionGroupRow
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TournamentID,
		&i.Category,
		&i.Pick,
		&i.GroupLetter,
		&i.PickName,
		&i.Points,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertPlayerPredictionNoGroup = `-- name: UpsertPlayerPredictionNoGroup :one
WITH ins AS (
    INSERT INTO player_predictions (user_id, tournament_id, category, pick)
    VALUES ($1, $2, $3, $4)
    ON CONFLICT (user_id, tournament_id, category)
        WHERE group_letter IS NULL
    DO UPDATE SET pick = EXCLUDED.pick, updated_at = now()
    RETURNING id, user_id, tournament_id, category, pick, group_letter, points, created_at, updated_at
)
SELECT ins.id, ins.user_id, ins.tournament_id, ins.category, ins.pick,
       ins.group_letter, p.name AS pick_name, ins.points, ins.created_at, ins.updated_at
FROM ins JOIN players p ON p.id = ins.pick
`

type UpsertPlayerPredictionNoGroupParams struct {
	UserID       uuid.UUID
	TournamentID uuid.UUID
	Category     PlayerHandicapCategory
	Pick         uuid.UUID
}

type UpsertPlayerPredictionNoGroupRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	TournamentID uuid.UUID
	Category     PlayerHandicapCategory
	Pick         uuid.UUID
	GroupLetter  *string
	PickName     string
	Points       *int32
	CreatedAt    time.Time
	UpdatedAt    time.Time
}

func (q *Queries) UpsertPlayerPredictionNoGroup(ctx context.Context, arg UpsertPlayerPredictionNoGroupParams) (UpsertPlayerPredictionNoGroupRow, error) {
	row := q.db.QueryRow(ctx, upsertPlayerPredictionNoGroup,
		arg.UserID,
		arg.TournamentID,
		arg.Category,
		arg.Pick,
	)
	var i UpsertPlayerPredictionNoGroupRow
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TournamentID,
		&i.Category,
		&i.Pick,
		&i.GroupLetter,
		&i.PickName,
		&i.Points,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertTeamPredictionGroup = `-- name: UpsertTeamPredictionGroup :one
WITH ins AS (
    INSERT INTO team_predictions (user_id, tournament_id, category, pick, group_letter, slot_index)
    VALUES ($1, $2, $3, $4, $5, $6)
    ON CONFLICT (user_id, tournament_id, category, group_letter, slot_index)
        WHERE group_letter IS NOT NULL
    DO UPDATE SET pick = EXCLUDED.pick, updated_at = now()
    RETURNING id, user_id, tournament_id, category, pick, group_letter, slot_index, points, created_at, updated_at
)
SELECT ins.id, ins.user_id, ins.tournament_id, ins.category, ins.pick,
       ins.group_letter, ins.slot_index, t.name AS pick_name, ins.points, ins.created_at, ins.updated_at
FROM ins JOIN teams t ON t.id = ins.pick
`

type UpsertTeamPredictionGroupParams struct {
	UserID       uuid.UUID
	TournamentID uuid.UUID
	Category     TeamHandicapCategory
	Pick         uuid.UUID
	GroupLetter  *string
	SlotIndex    int16
}

type UpsertTeamPredictionGroupRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	TournamentID uuid.UUID
	Category     TeamHandicapCategory
	Pick         uuid.UUID
	GroupLetter  *string
	SlotIndex    int16
	PickName     string
	Points       *int32
	CreatedAt    time.Time
	UpdatedAt    time.Time
}

func (q *Queries) UpsertTeamPredictionGroup(ctx context.Context, arg UpsertTeamPredictionGroupParams) (UpsertTeamPredictionGroupRow, error) {
	row := q.db.QueryRow(ctx, upsertTeamPredictionGroup,
		arg.UserID,
		arg.TournamentID,
		arg.Category,
		arg.Pick,
		arg.GroupLetter,
		arg.SlotIndex,
	)
	var i UpsertTeamPredictionGroupRow
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TournamentID,
		&i.Category,
		&i.Pick,
		&i.GroupLetter,
		&i.SlotIndex,
		&i.PickName,
		&i.Points,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertTeamPredictionNoGroup = `-- name: UpsertTeamPredictionNoGroup :one
WITH ins AS (
    INSERT INTO team_predictions (user_id, tournament_id, category, pick, slot_index)
    VALUES ($1, $2, $3, $4, $5)
    ON CONFLICT (user_id, tournament_id, category, slot_index)
        WHERE group_letter IS NULL
    DO UPDATE SET pick = EXCLUDED.pick, updated_at = now()
    RETURNING id, user_id, tournament_id, category, pick, group_letter, slot_index, points, created_at, updated_at
)
SELECT ins.id, ins.user_id, ins.tournament_id, ins.category, ins.pick,
       ins.group_letter, ins.slot_index, t.name AS pick_name, ins.points, ins.created_at, ins.updated_at
FROM ins JOIN teams t ON t.id = ins.pick
`

type UpsertTeamPredictionNoGroupParams struct {
	UserID       uuid.UUID
	TournamentID uuid.UUID
	Category     TeamHandicapCategory
	Pick         uuid.UUID
	SlotIndex    int16
}

type UpsertTeamPredictionNoGroupRow struct {
	ID           uuid.UUID
	UserID       uuid.UUID
	TournamentID uuid.UUID
	Category     TeamHandicapCategory
	Pick         uuid.UUID
	GroupLetter  *string
	SlotIndex    int16
	PickName     string
	Points       *int32
	CreatedAt    time.Time
	UpdatedAt    time.Time
}

func (q *Queries) UpsertTeamPredictionNoGroup(ctx context.Context, arg UpsertTeamPredictionNoGroupParams) (UpsertTeamPredictionNoGroupRow, error) {
	row := q.db.QueryRow(ctx, upsertTeamPredictionNoGroup,
		arg.UserID,
		arg.TournamentID,
		arg.Category,
		arg.Pick,
		arg.SlotIndex,
	)
	var i UpsertTeamPredictionNoGroupRow
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TournamentID,
		&i.Category,
		&i.Pick,
		&i.GroupLetter,
		&i.SlotIndex,
		&i.PickName,
		&i.Points,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
