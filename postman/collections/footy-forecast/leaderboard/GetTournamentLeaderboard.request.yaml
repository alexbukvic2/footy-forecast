$kind: http-request
name: Get Tournament Leaderboard
method: GET
url: '{{baseUrl}}/tournaments/{{tournamentId}}/leaderboard'
order: 2000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'

