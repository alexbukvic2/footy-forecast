$kind: http-request
name: List Score Predictions
method: GET
url: '{{baseUrl}}/leagues/{{leagueId}}/predictions'
auth:
  type: noauth
order: 6000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
