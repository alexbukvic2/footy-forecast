name: List Predictions (league view)
request:
  method: GET
  url:
    raw: "{{baseUrl}}/leagues/{{leagueId}}/predictions"
    host:
      - "{{baseUrl}}"
    path:
      - leagues
      - "{{leagueId}}"
      - predictions
  header:
    - key: Authorization
      value: "Bearer {{authToken}}"
