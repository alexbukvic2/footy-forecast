$kind: http-request
name: List League Player Predictions
method: GET
url: "{{baseUrl}}/leagues/{{leagueId}}/predictions/players"
auth:
  type: noauth
order: 3000
headers:
  - key: Authorization
    value: "Bearer {{authToken}}"
