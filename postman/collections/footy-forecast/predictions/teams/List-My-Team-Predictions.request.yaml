$kind: http-request
name: List My Team Predictions
method: GET
url: "{{baseUrl}}/tournaments/{{tournamentId}}/predictions/teams"
auth:
  type: noauth
order: 2000
headers:
  - key: Authorization
    value: "Bearer {{authToken}}"
