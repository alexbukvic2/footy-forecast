name: List Fixtures (personal view)
request:
  method: GET
  url:
    raw: "{{baseUrl}}/tournaments/{{tournamentId}}/fixtures"
    host:
      - "{{baseUrl}}"
    path:
      - tournaments
      - "{{tournamentId}}"
      - fixtures
  header:
    - key: Authorization
      value: "Bearer {{authToken}}"
