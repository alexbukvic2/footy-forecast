$kind: http-request
name: Complete Profile (Set Display Name)
method: PATCH
url: "{{baseUrl}}/users/me"
order: 2000
headers:
  Authorization: Bearer {{authToken}}
  Content-Type: application/json
body:
  mode: raw
  raw: |
    {
      "display_name": "{{displayName}}"
    }
